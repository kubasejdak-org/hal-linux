hal-trigger:
  stage: trigger
  #trigger: kubasejdak-libs/hal
  trigger:
    project: kubasejdak-libs/hal
    branch: maintenance/add-support-for-triggered-ci
  variables:
    TRIGGER_SOURCE_BRANCH: ${$CI_COMMIT_REF_NAME}
